# Skill 维护与同步

本仓库的远端写入权限来自用户当前或此前明确给出的持续授权；本文件本身不授予发布权限。已有持续同步授权时，完成更新后主动提交并推送，无需重复确认。

1. 修改前检查远端、当前分支、工作区及暂存区，获取远端最新状态；工作区干净且可快进时先更新本地分支。保留已有改动，不覆盖其他任务的工作。
2. 用户已确认的公开及持续同步范围为 `toy-model-info/`、`toy-cover/`、`ego-publish/` 及直接相关的仓库说明。安装位置若为软链接，编辑其仓库内源文件，不将链接替换成独立副本。
3. 完成 skill 修改后，用当前环境 `skill-creator` 的 `quick_validate.py` 校验受影响 skill；脚本变更还须运行相关测试。`ego-publish` 现有测试为 `python3 -B -m unittest discover -s ego-publish/scripts -p 'test_*.py'`。仅维护 skill 时不生成封面或发布社交平台测试内容。
4. 检查实际差异，只提交本次已授权的文件或改动块；不要使用 `git add .` 或夹带已有暂存项。公开仓库不加入照片、生成封面、发布记录、登录信息和本机私有路径。
5. 有实际改动且校验通过后，提交清晰的说明并推送至用户确认的分支；持续同步目标为 `origin/main` 时执行 `git push origin main`。无差异则不创建空提交。远端领先或推送失败时获取最新状态并处理可安全解决的冲突；不强推、不丢弃用户改动，不把本地提交成功当作远端已更新。
6. 用远端分支 SHA 核实提交已到达，报告提交链接。发生权限、校验或无法安全处理的冲突时保留本地结果，说明尚未同步及原因。
